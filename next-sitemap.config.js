/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'oceanconnecting.org', // Replace with your actual domain
    generateRobotsTxt: true, // Generates a robots.txt file
    sitemapSize: 5000, // (optional) Max number of URLs per sitemap file
  };
  